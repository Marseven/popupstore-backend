; Raise upload limits so large media files (audio/video) can be uploaded
; through the admin. Read by PHP-FPM / LiteSpeed from the API document root.
upload_max_filesize = 128M
post_max_size = 128M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
